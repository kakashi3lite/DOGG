# Test Environment Configuration
# This file is loaded during test execution only

# JWT Configuration (test values - NOT for production)
JWT_SECRET=test-jwt-secret-key-for-testing-only-do-not-use-in-production-12345

# Database Configuration (handled by mongo-memory-server in setup.ts)
# No need to set MONGODB_URI as we use in-memory database

# API Configuration
NODE_ENV=test
PORT=3001

# Web App URL for CORS
WEB_APP_URL=http://localhost:3000
